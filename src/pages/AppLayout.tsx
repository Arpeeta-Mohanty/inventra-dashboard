/**
 * pages/AppLayout.tsx — compatibility re-export.
 *
 * This file is kept so that any tooling or stale import that still references
 * pages/AppLayout continues to work.  The real implementation now lives in:
 *   layouts/DashboardLayout.tsx
 */
export { default } from '../layouts/DashboardLayout';
